export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
  // Demo student shown on load. Not hardcoded dashboard data - this only selects
  // which student's real data to fetch from the API.
  studentId: 1
};
